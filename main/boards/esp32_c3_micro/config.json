{
    "target": "esp32c3",
    "builds": [
        {
            "name": "esp32-c3-micro",
            "sdkconfig_append": [
                "CONFIG_FREERTOS_USE_TICKLESS_IDLE=y",
                "CONFIG_USE_ESP_WAKE_WORD=y",
                "CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y"
            ]
        }
    ]
}

